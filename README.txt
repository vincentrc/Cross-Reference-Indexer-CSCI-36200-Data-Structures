Instructions:

First, please make sure all of the program source/header files are present 
in the directory, as well as the makefile and the file to be cross-referenced.

To compile the program, type "make".

After doing this, type "make run" to run the program.

The program will ask for the name of the file to be cross-referenced at runtime.